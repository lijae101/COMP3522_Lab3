//Name: Liam Janicke
//Student Number: A01382890

#include <iostream>
#include "myStack.hpp"0

/*int main() {
    MyStack stack;

    // Test if the stack is initially empty
    std::cout << "Empty: " << (stack.empty() ? "true" : "false") << std::endl;
    std::cout << "Full: " << (stack.full() ? "true" : "false") << std::endl;

    // Test pushing elements
    std::cout << "\nPushing elements\n";
    stack.push(10);
    stack.push(20);
    stack.push(30);
    std::cout << "Stack contents: " << stack.print() << std::endl;

    // Test top element
    std::cout << "\nTesting top element\n";
    std::cout << "Top: " << stack.top() << std::endl;

    // Test popping elements
    std::cout << "\nTesting pop() elements\n";
    stack.pop();
    std::cout << "Stack contents after pop: " << stack.print() << std::endl;
    std::cout << "Top after pop: " << stack.top() << std::endl;

    // Test pushing to full stack
    std::cout << "Testing full condition\n";
    for (int i = 0; i < 10; ++i) {
        stack.push(i * 10);
    }
    std::cout << "Stack contents when full: " << stack.print() << std::endl;
    std::cout << "Push result when full: " << (stack.push(100) ? "Success" : "Failed") << std::endl;

    // Test popping from empty stack
    std::cout << "\nPop from empty stack\n";
    for (int i = 0; i < 10; ++i) {
        stack.pop();
    }
    std::cout << "Stack contents after popping all elements: " << stack.print() << std::endl;
    std::cout << "Top after popping all elements: " << stack.top() << std::endl;

    return 0;
}
*/